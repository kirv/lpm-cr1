#!/usr/local/bin/bash

main() {
    local site
    local -a chunks

    site="$1" && shift

    chunks=(
        constants-table
        constants
        globals
        datatables
        functions
        subs
        programs
    )
    
    for chunk in "${chunks[@]}"; do  
        $chunk $site "$@"
    done
}

main "$@"


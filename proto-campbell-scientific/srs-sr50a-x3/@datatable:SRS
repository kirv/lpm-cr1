dataTable(SRS,1,-1)
    sample(1,srs1_dist_raw,fp2)
    sample(1,srs1_qual,fp2)
    sample(1,srs2_dist_raw,fp2)
    sample(1,srs2_qual,fp2)
    sample(1,srs3_dist_raw,fp2)
    sample(1,srs3_qual,fp2)
endTable

dataTable(SRS,1,-1)
    sample(srs1_dist_raw,fp2)
    sample(srs1_qual,fp2)
    sample(srs2_dist_raw,fp2)
    sample(srs2_qual,fp2)
    sample(srs3_dist_raw,fp2)
    sample(srs3_qual,fp2)
endTable
